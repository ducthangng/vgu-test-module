import React from 'react';

interface Result {
  id: number;
  testClassId: number;
  userId: number;
  entityCode: number;
  dateCreated: number;
  score: number;
  comment: string;
  resultNote: string;
}

export type { Result };
